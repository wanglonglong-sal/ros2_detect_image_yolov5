object_tracker:
  ros__parameters:
    # Toggle writing the tracked video
    enable_output_video: true

    # Default output path and fps (can be overridden by launch arg)
    output_video_path: "/mnt/d/Dataset/Output/tracked_output.mp4"
    output_fps: 30.0
    log_detail: false
